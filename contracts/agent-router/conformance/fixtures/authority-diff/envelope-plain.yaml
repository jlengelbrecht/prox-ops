# Seeded fixture: the catalog document from envelope-configmap.yaml, bare.
# Same content, no ConfigMap around it. Expected against that pair: neutral in
# both directions.
version: "1.3.0"
schema_version: 1
profiles:
  local-code-standard:
    description: The default local coding profile.
    selectable: true
    capabilities: [chat, tools]
    min_context: 65536
    forbidden_for: []
    physical:
      - model_id: qwen36-27b
        placement: kserve-a5000
