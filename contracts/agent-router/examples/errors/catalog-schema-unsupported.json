{
  "error": {
    "code": "catalog_schema_unsupported",
    "message": "The mounted catalog declares schema_version 2 and this router implements 1. It refuses the document rather than best-effort parsing a shape it does not understand, because a partially-understood catalog produces confidently wrong placements. This will not clear on retry: an operator has to deploy a router that implements the newer schema, or revert the catalog. Abort the attempt and alert rather than backing off in a loop.",
    "obligation": "abort",
    "http_status": 503,
    "endpoint": "/v1/status",
    "details": {
      "received_catalog_schema_version": 2,
      "supported_catalog_schema_version": 1
    }
  }
}
