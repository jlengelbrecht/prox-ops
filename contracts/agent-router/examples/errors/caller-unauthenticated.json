{
  "error": {
    "code": "unauthenticated",
    "message": "Missing or invalid caller bearer credential. This is the caller credential (planning and dispatch clients), not the per-node edge credential: the two are separate schemes on purpose, so a client that can ask for a placement cannot report capacity. No endpoint is named here because the same envelope is served from every caller-facing endpoint; retrying with the same credential produces lockout and log noise, not access.",
    "obligation": "abort",
    "http_status": 401
  }
}
