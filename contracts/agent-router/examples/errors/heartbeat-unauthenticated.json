{
  "error": {
    "code": "unauthenticated",
    "message": "Missing or invalid bearer credential. Heartbeats are authenticated per node; a rejected credential is never treated as a heartbeat from an unknown node.",
    "obligation": "abort",
    "http_status": 401,
    "endpoint": "/v1/capacity/heartbeat"
  }
}
