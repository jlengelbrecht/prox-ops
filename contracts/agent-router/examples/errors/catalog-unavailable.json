{
  "error": {
    "code": "catalog_unavailable",
    "message": "No usable catalog is loaded yet; the router has started but has not read the mounted document. No catalog_version is reported because there is nothing to hash. The same code covers a catalog whose schema_version this router does not implement - it refuses rather than best-effort parsing an unknown shape - and that variant will not clear on retry without an operator.",
    "obligation": "retry",
    "http_status": 503,
    "endpoint": "/v1/status",
    "retry_after_seconds": 5
  }
}
