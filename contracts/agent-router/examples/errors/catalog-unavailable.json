{
  "error": {
    "code": "catalog_unavailable",
    "message": "No catalog is loaded yet. The router has started but has not finished reading the mounted document, so it has nothing to answer from and no catalog_version to report - there is nothing to hash. This is the TRANSIENT case only: the same request will succeed unchanged once the load completes, which is why the obligation is retry. A catalog the router loaded and then refused because its schema_version is one this build does not implement is a different code, catalog_schema_unsupported, precisely because retrying that one forever is the wrong behaviour.",
    "obligation": "retry",
    "http_status": 503,
    "endpoint": "/v1/status",
    "retry_after_seconds": 5
  }
}
