# Agent router contracts

The agent-router contracts now live in `jlengelbrecht/ai-control-plane` under `contracts/agent-router/`, starting with release `v0.1.0`. Nothing in this directory is verified by CI anymore.

This directory retains only validation evidence cited by the prox-ops agent-router catalog. The cited evidence remains here until the product repository imports it.
