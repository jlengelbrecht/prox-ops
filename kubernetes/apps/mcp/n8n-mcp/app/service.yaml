---
apiVersion: v1
kind: Service
metadata:
  name: n8n-mcp
  namespace: mcp
  labels:
    app.kubernetes.io/name: n8n-mcp
    app.kubernetes.io/component: mcp-server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: n8n-mcp
