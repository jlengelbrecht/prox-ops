---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - mcpgroup.yaml
  # MCPServer resources (ToolHive-managed MCP servers)
  # Note: graphiti uses standalone HelmRelease due to complex image requirements
  # TODO: Migrate graphiti to MCPServer when ToolHive supports init containers better
  - mcpserver-n8n.yaml
  - mcpexternalauthconfig-n8n.yaml
  - externalsecret-n8n.yaml
  - virtualmcpserver.yaml
  - networkpolicy.yaml
  - networkpolicy-mcpservers.yaml
