---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - mcpgroup.yaml
  # n8n switched to standalone deployment due to ToolHive headerInjection bug (#3111)
  # MCPServer n8n removed - see ../n8n-mcp/ for standalone deployment
  # ExternalSecret kept here as it's shared by standalone n8n-mcp
  - externalsecret-n8n.yaml
  - virtualmcpserver.yaml
  - networkpolicy.yaml
  - networkpolicy-mcpservers.yaml
