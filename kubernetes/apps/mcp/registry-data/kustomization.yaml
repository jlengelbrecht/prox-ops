---
# Kustomization for MCP Registry Data
# This directory contains spec.yaml files for MCP servers
# Read by the ToolHive registry-server via Git source
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# No Kubernetes resources to deploy - this is just data for the registry
# The registry-server reads directly from Git
resources: []
