---
# Note: This PVC is managed by Grafana's persistence settings in HelmRelease values
# This file documents the configuration but the actual PVC is created by the Deployment
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: kube-prometheus-stack-grafana
#   namespace: observability
# spec:
#   accessModes:
#     - ReadWriteOnce
#   storageClassName: ceph-block
#   resources:
#     requests:
#       storage: 10Gi
