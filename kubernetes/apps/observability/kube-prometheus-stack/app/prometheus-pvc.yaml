---
# Note: This PVC is managed by the Prometheus StatefulSet via volumeClaimTemplate
# This file documents the configuration but the actual PVC is created by the StatefulSet
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: prometheus-kube-prometheus-stack-prometheus-db-prometheus-kube-prometheus-stack-prometheus-0
#   namespace: observability
# spec:
#   accessModes:
#     - ReadWriteOnce
#   storageClassName: ceph-block
#   resources:
#     requests:
#       storage: 100Gi
