---
apiVersion: v1
kind: Service
metadata:
  name: alertmanager-discord
  namespace: observability
  labels:
    app.kubernetes.io/name: alertmanager-discord
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9094
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: alertmanager-discord
