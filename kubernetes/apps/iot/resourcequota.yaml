---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: iot-quota
  namespace: iot
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    persistentvolumeclaims: "5"
    services.loadbalancers: "3"
