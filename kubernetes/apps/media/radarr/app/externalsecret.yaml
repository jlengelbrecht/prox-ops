---
# ExternalSecret for Radarr API key - used by exportarr sidecar
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: radarr-exporter
  namespace: media
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: radarr-exporter
    creationPolicy: Owner
    deletionPolicy: Retain
  refreshInterval: 1h
  data:
    - secretKey: API_KEY
      remoteRef:
        key: radarr-api
        property: api-key
