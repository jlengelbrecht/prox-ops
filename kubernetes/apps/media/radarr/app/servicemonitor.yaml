---
# ServiceMonitor for Radarr exportarr metrics
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: radarr
  namespace: media
  labels:
    app.kubernetes.io/name: radarr
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: radarr
  namespaceSelector:
    matchNames:
      - media
  endpoints:
    - port: metrics
      interval: 4m
      scrapeTimeout: 90s
      path: /metrics
