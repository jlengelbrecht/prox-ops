---
# ExternalSecret for Prowlarr API key - used by exportarr sidecar
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: prowlarr-exporter
  namespace: media
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: prowlarr-exporter
    creationPolicy: Owner
    deletionPolicy: Retain
  refreshInterval: 1h
  data:
    - secretKey: API_KEY
      remoteRef:
        key: prowlarr-api
        property: api-key
