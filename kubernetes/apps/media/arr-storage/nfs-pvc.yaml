---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: arr-media
  namespace: media
  labels:
    app.kubernetes.io/name: arr-stack
    app.kubernetes.io/component: storage
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: arr-media-nfs
  resources:
    requests:
      storage: 50Ti
