---
# CRITICAL: prune: false prevents Flux from deleting PVCs during reconciliation
# This protects FileFlows config data from accidental deletion
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: fileflows-storage
  namespace: flux-system
spec:
  interval: 1h
  path: ./kubernetes/apps/media/fileflows/storage
  postBuild:
    substituteFrom:
      - name: cluster-secrets
        kind: Secret
  prune: false  # NEVER prune storage - prevents data loss
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: media
  wait: true
