---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Core cluster resources
  - externalsecret.yaml
  - helmrelease.yaml
  - networkpolicy.yaml
  # Application databases (declaratively managed via Database CRD)
  # Note: firecrawl database removed (STORY-062) - using Firecrawl.dev cloud API
  - database-toolhive.yaml
  - externalsecret-toolhive.yaml
  - database-zabbix.yaml
  - externalsecret-zabbix.yaml
