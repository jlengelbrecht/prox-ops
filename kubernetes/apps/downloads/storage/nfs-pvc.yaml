---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: downloads-media
  namespace: downloads
  labels:
    app.kubernetes.io/name: downloads
    app.kubernetes.io/component: storage
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: downloads-media-nfs
  resources:
    requests:
      storage: 50Ti
