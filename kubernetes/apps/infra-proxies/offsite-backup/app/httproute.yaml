---
# HTTPRoute for Proxmox Backup Server (offsite)
# Provides TLS termination via envoy-internal gateway and auto-DNS via external-dns
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: offsite-backup
  namespace: infra-proxies
  annotations:
    external-dns.alpha.kubernetes.io/hostname: offsite-backup.homelab0.org
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - "offsite-backup.homelab0.org"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: gateway.envoyproxy.io
          kind: Backend
          name: offsite-backup
