---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: truenas
  namespace: flux-system
spec:
  targetNamespace: infra-proxies
  commonMetadata:
    labels:
      app.kubernetes.io/name: truenas
  dependsOn:
    - name: infra-proxies-base
    - name: envoy-gateway-config
      namespace: network
  path: ./kubernetes/apps/infra-proxies/truenas/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: true
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  healthChecks:
    - apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      name: truenas-heimdall
      namespace: infra-proxies
    - apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      name: truenas-baldar
      namespace: infra-proxies
