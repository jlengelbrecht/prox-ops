---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - backends.yaml
  - httproute.yaml
  - policy.yaml
  # STORY-034-11 CLEANUP: referencegrant.yaml removed - it existed only
  # for the DEGRADED rule's cross-namespace Service backendRef, which was
  # removed in the same change (see httproute.yaml). Nothing else
  # referenced it (verified by repo-wide grep before removal).
