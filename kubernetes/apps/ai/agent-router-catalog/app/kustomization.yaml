---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # STORY-035-1: model & capability catalog (schema + seed). Data only -
  # this Kustomization intentionally ships no workload. README.md is the
  # schema document and is deliberately NOT a resource.
  - catalog-configmap.yaml
