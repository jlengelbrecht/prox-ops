---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ai

resources:
  - ./namespace.yaml
  # EPIC-022 AI Platform - BLOCKED pending PM architectural review
  # Critical flaws identified: llama-swap not K8s-native, no hot-swap path
  # See: .claude/.ai-docs/epics/EPIC-022-local-ai-platform.md
  #
  # vLLM removed - awaiting PM decision on Ollama vs vLLM architecture
  # Open WebUI - STORY-073 (blocked)
  # Qdrant - STORY-074 (blocked)
  # LiteLLM - STORY-075 (blocked)
