---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: localrecall
  namespace: ai
  annotations:
    external-dns.alpha.kubernetes.io/hostname: localrecall.homelab0.org
  labels:
    app.kubernetes.io/name: localrecall
    app.kubernetes.io/instance: localrecall
spec:
  parentRefs:
    - name: envoy-internal
      namespace: network
      sectionName: https
  hostnames:
    - "localrecall.homelab0.org"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: localrecall
          port: 8080
      filters:
        - type: ResponseHeaderModifier
          responseHeaderModifier:
            set:
              - name: Strict-Transport-Security
                value: "max-age=31536000; includeSubDomains"
              - name: X-Frame-Options
                value: "DENY"
              - name: X-Content-Type-Options
                value: "nosniff"
              - name: Referrer-Policy
                value: "strict-origin-when-cross-origin"
