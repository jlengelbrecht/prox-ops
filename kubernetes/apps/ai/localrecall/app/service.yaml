---
apiVersion: v1
kind: Service
metadata:
  name: localrecall
  namespace: ai
  labels:
    app.kubernetes.io/name: localrecall
    app.kubernetes.io/instance: localrecall
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: localrecall
    app.kubernetes.io/instance: localrecall
